

-- MACHINE GENERATED -- DO NOT EDIT
-- This schedule was automatically generated by (None)
-- for target=arch_unknown-0-os_unknown  -- NOLINT
-- with machine_params=(None)

    -- schedule
    -- xi, yi = hl.Var("xi"), hl.Var("yi")
    -- blur_y.tile(x, y, xi, yi, 8, 4).parallel(y).vectorize(xi, 8)
    -- blur_x.compute_at(blur_y, x).vectorize(x, 8)


apply_schedule_blur = function( pipeline, target) 
    -- --- BEGIN machine-generated schedule
    blur_y = pipeline:get_func(3)
    blur_x = pipeline:get_func(2)
    input_uint16 = pipeline:get_func(1)
    input = pipeline:get_func(0)
    x = Var_new(blur_y:get_schedule():dims()[1]:var())
    xi = Var_new("xi")
    y = Var_new(blur_y:get_schedule():dims()[2]:var())
    yi = Var_new("yi")
    yii = Var_new("yii")
    blur_y
        :tile(x, y, xi, yi, 8, 4)
        :parallel(y)
        :vectorize(xi, 8)
--    blur_x
--        :compute_at(blur_y, x)
--        :vectorize(x, 8)
--        :store_in(MemoryType.Stack)
--        :split(x, x, xi, 8, TailStrategy.RoundUp)
--        :vectorize(xi)
--        :reorder( xi, x, y )
    -- --- END machine-generated schedule
end
