/* Empty stub: the runtimeadd_module shared library is built entirely from the
 * WHOLE_ARCHIVE'd AOT static library. A CMake MODULE library still needs at
 * least one source file of its own, which this provides. */
