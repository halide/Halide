- add stencil to lesson02 - show need for clamp on input to avoid bounds assertions
- reductions
- debug_to_file
- scheduling
- lambdas  
  useful tricks, e.g., `Image<float> image_buf = lambda(x, y, (sin(x+y)+1)/2).realize(10 * tile_size, 10 * tile_size);`
